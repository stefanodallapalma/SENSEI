swagger: "2.0"
info:
  description: This is the swagger file that goes with our server code
  version: "1.0.0"
  title: Swagger REST Article
consumes:
  - "application/json"
produces:
  - "application/json"

basePath: "/"

# Paths supported by the server application
paths:
  # EXAMPLE OF PATH
  /scraper:
    post:
      operationId: "api.scraper.berlusconi_vendor_list"
      summary: "The people data structure supported by the server application"
      consumes:
        - application/form-data
      
      parameters:
        - in:  formData
          name: url
          type: string
          #schema:
            
      
      responses:
        200:
          description: "Successful read fruit list operation"
          schema:
            type: boolean

